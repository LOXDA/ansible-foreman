---

- hosts: ldapservers
  gather_facts: true
  roles:
    - role: ansible-openldap

- hosts: ldapservers
  gather_facts: true
  become: true
  roles:
    - icinga.icinga.repos
    - icinga.icinga.icinga2
  tags:
    - never
    - monitoring
